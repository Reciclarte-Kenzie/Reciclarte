import Header from "../../components/Header";
import { Profile as Container } from "./styles";

const Profile = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Profile;
