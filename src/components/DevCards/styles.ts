import styled from "styled-components";

export const DevCardStyled = styled.li`
  .externalLinks {
    display: flex;
    flex-direction: column;
    gap: 10px;

    a {
      color: inherit;
    }
  }
`